<%= form_with(model: post_invitation, local: true) do |form| %>
  <% if post_invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_invitation.errors.count, "error") %> prohibited this post_invitation from being saved:</h2>

      <ul>
      <% post_invitation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id, id: :post_invitation_post_id %>
  </div>

  <div class="field">
    <%= form.label :teenager_id %>
    <%= form.text_field :teenager_id, id: :post_invitation_teenager_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
